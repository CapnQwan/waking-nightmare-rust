# Rendering Module

This module contains elements to help in rendering. 
Intended to be used with either the `gl_renderer` or `vulkan_renderer` modules.

This module contains useful structs such as Color, Mesh, Material, etc.
