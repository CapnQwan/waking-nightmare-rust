enum AccessPattern {
    ReadWorld,
    WriteWorld,
    ReadOnly,
}

trait System {
    // fn phase(&self) -> Phase;
    // fn run(&mut self, ctx: &mut Context);
    // fn access(&self) -> AccessPattern;
}